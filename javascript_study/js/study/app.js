const a = 5 ;
const b = 2 ;

let myName = "jihyeon"

console.log(a+b);
console.log(a*b);
console.log(a/b);
console.log("hello " + myName)

myName = "jeong ji hyeon"
console.log("your name is "+myName)
/*
    const는 constant(상수)이며 값이 바뀔 수 없다(업데이트 안됨)
*/
/* 
    let은 새로운값을 업데이트할때 쓰임
*/

const amIFat = null;
let something; /* undefined */
console.log(something,amIFat);

/* 
 true  = 켜져있음
 false = 꺼져있음
 null = 아무것도 없음
 "const or let 에 값을 주고 아무것도 입력하지 않았다면
 undefined = 인지하지만 값은 없는 상태"
*/

